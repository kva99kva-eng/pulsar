# Data

Raw FITS files are not included in this repository because they may be large and are ignored by Git.

To run the notebooks, place the photon event file here:

```text
data/raw/vela_photons.fits
Expected local structure:

data/
└── raw/
    └── vela_photons.fits

The notebooks use this file path:

../data/raw/vela_photons.fits

